apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "twitter_analysis.fullname" . }}-test-connection"
  labels:
    {{- include "twitter_analysis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "twitter_analysis.fullname" . }}']
  restartPolicy: Never

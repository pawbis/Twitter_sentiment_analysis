apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "twitter_vis.fullname" . }}-test-connection"
  labels:
    {{- include "twitter_vis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "twitter_vis.fullname" . }}']
  restartPolicy: Never
